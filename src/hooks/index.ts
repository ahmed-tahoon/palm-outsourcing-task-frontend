/**
 * Hooks Barrel Export
 *
 * Central export point for all custom hooks to maintain
 * clean imports and better developer experience.
 */

export { useProducts } from "./useProducts";

// Export types for external consumption
export type { UseProductsReturn, UseProductReturn } from "@/types/Product";
